// Function OnClick ( Element 2 )

let text = document.getElementById("change1"); // Get the text element

function change() {
    text.innerHTML = "text was changed"; // Update the text content when called
};